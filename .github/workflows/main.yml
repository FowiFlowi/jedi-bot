name: Deploy

on: [push]

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:
    - name: SSH Commands
      run: echo ${{ secrets.HOST }}
#       uses: appleboy/ssh-action@v0.0.2
#       with:
#         host: ${{ secrets.HOST }}
#         username: ${{ secrets.USERNAME }}
#         key: ${{ secrets.SSH_KEY }}
#         script: echo ${{ secrets.APP_PATH }}
